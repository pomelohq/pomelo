name: Rust Release

# Push a `rust-v*` tag (make patch|minor|major in rust/) -> build the release bundle, sign it with the
# Developer ID, notarize the DMG, and publish the DMG plus the in-app update tarball as a GitHub Release.
# Uses the same signing and notarization secrets as the Swift app's release.
on:
  push:
    tags: ['rust-v*']

defaults:
  run:
    working-directory: rust

permissions:
  contents: write # create the GitHub Release + upload assets

jobs:
  release:
    runs-on: macos-15 # Apple Silicon
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: rust

      - name: Import Developer ID certificate
        env:
          CERT_P12: ${{ secrets.MACOS_CERT_P12 }}
          CERT_PASSWORD: ${{ secrets.MACOS_CERT_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          KC="$RUNNER_TEMP/build.keychain"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KC"
          security set-keychain-settings -lut 21600 "$KC"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KC"
          echo "$CERT_P12" | base64 --decode > "$RUNNER_TEMP/cert.p12"
          security import "$RUNNER_TEMP/cert.p12" -k "$KC" -P "$CERT_PASSWORD" \
            -T /usr/bin/codesign -T /usr/bin/security
          security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" "$KC"
          security list-keychains -d user -s "$KC" $(security list-keychains -d user | tr -d '"')
          rm -f "$RUNNER_TEMP/cert.p12"

      - name: Store notarization credentials
        env:
          NOTARY_APPLE_ID: ${{ secrets.NOTARY_APPLE_ID }}
          NOTARY_APP_PASSWORD: ${{ secrets.NOTARY_APP_PASSWORD }}
          NOTARY_TEAM_ID: ${{ secrets.NOTARY_TEAM_ID }}
        run: |
          xcrun notarytool store-credentials pomelo-notary \
            --apple-id "$NOTARY_APPLE_ID" \
            --password "$NOTARY_APP_PASSWORD" \
            --team-id "$NOTARY_TEAM_ID"

      - name: Build, sign and notarize the DMG
        env:
          SIGN_ID: ${{ secrets.MACOS_SIGN_IDENTITY }}
          NOTARY_PROFILE: pomelo-notary
        run: scripts/make-dmg.sh

      - name: Build the auto-update artifacts
        run: scripts/make-appcast.sh

      - name: Checksums
        run: |
          cd target
          shasum -a 256 Pomelo-*.dmg Pomelo-*.app.tar.gz latest.json > checksums.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            rust/target/Pomelo-*.dmg
            rust/target/Pomelo-*.app.tar.gz
            rust/target/latest.json
            rust/target/checksums.txt
          fail_on_unmatched_files: true
          generate_release_notes: true

      - name: Clean up keychain
        if: ${{ always() }}
        run: security delete-keychain "$RUNNER_TEMP/build.keychain" 2>/dev/null || true
