[package]
name = "pom_sessions"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Session registry (sessions.json) and session-to-project registry (registry.json)."

[lib]
path = "src/pom_sessions.rs"

[dependencies]
pom_paths = { path = "../pom_paths" }
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
tempfile = "3"
