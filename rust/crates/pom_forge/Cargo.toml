[package]
name = "pom_forge"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reading pull request status from GitHub: the token, the batched GraphQL lookups, the status tokens the UI colours by, and a disk cache shared with the previous core."

[lib]
path = "src/pom_forge.rs"

[dependencies]
pom_paths = { path = "../pom_paths" }
pom_secrets = { path = "../pom_secrets" }
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
tempfile = "3"
